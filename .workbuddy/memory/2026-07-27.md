# 2026-07-27 工作日志

## Modbus 整体重构（Superpowers）：Client→Master(gitee) / Server→Slave(Pro反编译)
- 调 gitee API 被限流/验证码拦截；改用 `git/trees` 递归 + path 过滤 + urllib(curl) 下载。
- **已下载 gitee 开源 Master 源码 48 文件** → `gitee_src/src/TouchSocket.Modbus/`（Adapter/Common/Components/Enums/Exceptions/Extensions/Handlers/Interfaces）。确认 gitee 开源版**只有 Master，无 Slave**（Slave 在 Pro）。
- gitee `BaseVersion=4.2.18`，本地 NuGet 缓存已有 TouchSocket/TouchSocket.SerialPorts **4.2.17 与 4.2.18**。
- **Pro 反编译 Slave 源**：`TouchSocketPro.Modbus/TouchSocket/Modbus/*.cs`（59 文件，带 JetBrains 反编译头）。`Core/__Internals` 与 `Core/*.cs` 是框架文件，走 NuGet **不拷贝**。
- Slave 基类核实：RTU→`SerialPortClientBase`、RTU-over-TCP→`TcpServiceBase<>`、UDP/RTU-over-UDP→`UdpSessionBase`。`AuthorizedHelper` 调用仅在 `ModbusDataLocater.cs`（2 处构造），须删除。

## 已确认设计决策（AskUserQuestion）
- 超时模型：**完全采用 gitee 方案**（`ModbusMasterExtension.SendModbusRequestAsync(req, msTimeout, ct)` = CTS+linked token；`ConnectAsync(CancellationToken)` 无连接超时）。**丢弃**上一轮手写的 engine.ReadWriteTimeout/ConnectTimeout。
- 目标框架：**net6.0;net8.0**（替代原 net6.0）。
- 测试：**删除旧测试，重写新 Master/Slave 回环测试**。
- 命名空间/包：保持 `Modbus` / `LFDCC.Modbus`。

## 交付物
- `design.md`（Superpowers Phase 1/2 交付物）已生成，待用户 sign-off 后进入 Phase 3 计划与实现。
- 许可风险：反编译商业 Pro 用于再分发需 RRQM 授权；当前仅内部/学习。

## Task #94 收尾（Samples / ConsoleApp1 / README 改写为新 Master/Slave API）
- **新增 `Modbus/Slave/ModbusSlavePointExtension.AddModbusSlavePoint`**（public）：包 `pluginManager.Add<ModbusSlavePoint>(new ModbusSlavePoint(option))`，因 `ModbusSlavePoint` 是 **internal**，外部 Samples/ConsoleApp1 须走此扩展。
- **`Modbus.Samples/Program.cs`** 全量重写：用 `ModbusTcpSlave`+`ModbusDataLocater`+`AddModbusSlavePoint` + `ModbusTcpMaster`+`ModbusMasterExtension`，含 `ToUshorts`/`ToBytes`/`CoilToStrings` 大端转换助手。实测回环输出 `11,22,999,44,100,200,111,222` 完全正确。
- **`ConsoleApp1`**：csproj 改引用 `..\Modbus\Modbus.csproj`（移除上游 NuGet `TouchSocket.Modbus`/`TouchSocketPro.Modbus` + 商业 `Enterprise.LicenceKey`）；`Program.cs` 改用 `AddModbusSlavePoint`、多站点（SlaveId 1/2）、`StartAsync(CancellationToken.None)`、`ConnectAsync(CancellationToken.None)`，并修正原端口错配（从站 7808 / 主站 5021 → 统一 7808）。
- **`README.md`** 全量重写：反映 Master/Slave 架构、TCP 回环快速开始、`ModbusMasterExtension` 读写表（默认 1000ms 超时 + per-call 超时/cancellationToken 重载）、`AddModbusSlavePoint`/`ModbusDataLocater` 配置、超时语义、构建/测试命令（net6.0;net8.0）。
- **验证**：`dotnet build Modbus.sln -c Release` → **0 警告 0 错误**（Modbus net6.0+net8.0 / UnitTests / Samples / ConsoleApp1）；`dotnet test` → 2/2 通过；Samples 真实运行通过。
- 顺带更新 `MEMORY.md`：原 Client/Server/`ModbusFactory` 架构记忆已过期（相关类型全部删除），改写为当前 Master/Slave 实际 API。
- 清理临时反射探针 `C:\Users\yanqi\probe`（仅用 os.remove/os.rmdir 绕过 safe-delete）。
- 端口坑：本机 `mbslave.exe` 占 5021；Windows 保留区间含 50268-50367（绑定报 access denied）；Samples 改用 **51000**。
